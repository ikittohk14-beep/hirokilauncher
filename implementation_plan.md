# План реализации улучшений HirokiLauncher (по приоритету)

## 1. Визуальные и базовые исправления (Высокий приоритет)
- [ ] **Размер окна**: Обновить `src/main/index.ts`, установив стартовый размер окна (1150x750 или 1200x800), чтобы все кубы помещались сразу.
- [ ] **Отступы (Зазор)**: В `App.tsx` уменьшить padding между левой панелью навигации (`NavRail`) и основным контентом.
- [ ] **Баланс кубов**: В `BentoGrid.tsx` выровнять левую колонку (`SessionCard` и `HeatmapCard`), сделав их равными по высоте (по 50%), а также исправить пустоту в полке сборок (`InstanceShelf`), сделав карточки `InstTile` более пропорциональными.
- [ ] **Удаление лишнего текста**: Убрать "DRIFTWM SYNC ON" из `Titlebar.tsx` и "WAYLAND/PROXY OK" из карточки статистики.
- [ ] **Иконка аккаунта**: Исправить отображение аватара в `Accounts.tsx` (использовать правильный URL шлема Ely.by).

## 2. Улучшение каталога контента и установок (Высокий приоритет)
- [ ] **Поиск без Enter**: В `Catalog.tsx` добавить debounce для поля ввода, чтобы поиск происходил во время печати.
- [ ] **Сетка каталога**: Изменить сетку модов на 3 колонки (`grid-cols-3`) и обновить стили кнопок "Установить".
- [ ] **Прогресс-бар установки**: Добавить глобальный или локальный прогресс-бар внизу каталога, который появляется во время загрузки мода/сборки и исчезает после.

## 3. Настройки и карточка статистики (Средний приоритет)
- [ ] **Настройки (Auto-apply)**: В `Settings.tsx` убрать кнопку "Сохранить", растянуть контент на всю ширину (убрать пустую правую колонку) и сделать авто-сохранение при любом изменении.
- [ ] **Карточка контента (вместо ModsCard)**: Переделать `ModsCard.tsx` в карточку статистики сборки. Она будет показывать количество модов, шейдеров и ресурспаков. При клике на неё будет открываться страница управления сборкой (`InstanceView`).

## 4. Трекинг активности и кастомизация (Сложные задачи)
- [ ] **Трекер времени и Heatmap**: Написать логику учета времени в игре (сохранять сессии в `localStorage` или Electron store) и передавать реальные данные в `SessionCard` и `HeatmapCard`.
- [ ] **Редактор цвета (Theme Picker)**: В настройках добавить выбор главного акцентного цвета. При изменении цвета скрипт будет пересчитывать CSS переменные (`--blue`, `--blue-glow`).
- [ ] **Редактируемые иконки и Drag-and-Drop кубов**: Добавить возможность смены эмодзи для сборки. Реализовать простой Drag-and-Drop для перестановки виджетов на главном экране.

---

План составлен. Я начну выполнение с первого блока.
