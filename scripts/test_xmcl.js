import installer from '@xmcl/installer';
import core from '@xmcl/core';
console.log('Modules loaded successfully!');
console.log(Object.keys(installer).join(', '));
console.log(Object.keys(core).join(', '));
